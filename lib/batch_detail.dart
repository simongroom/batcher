import 'package:batcher/models/batch.dart';
import 'package:flutter/material.dart';

class BatchDetail extends StatelessWidget {
  final Batch batch;

  BatchDetail({this.batch});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
